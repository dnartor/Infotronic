<section class="offset-md-2 col-md-8 col-xs-12 custom-form">
  <div class="d-flex justify-content-center mt-5 content">
    <div class="col-md-10 col-xs-12 py-4">
      <div class="row">
        <div class="col-12 mt-2">
          <h2 class="text-center">Reclamaciones</h2>
          <table class="table table-hover mt-3">
            <thead class = "thead-dark">
            <tr>
              <th class="text-center"scope="col">Description</th>
            </tr>
            </thead>
            <tbody>
              <% @claims.each do |claim| %>
                <% if current_account.id == claim.account_id %>
                  <tr>
                    <td><%= claim.description %></td>
<!--                <td><%#= claim.account_id %></td>-->
                  <!--current_account.id-->
                    <!--<td><%#= link_to 'Show', claim %></td>-->
                    <!--<td><%#= link_to 'Edit', edit_claim_path(claim) %></td>-->
                    <!--<td><%#= link_to 'Destroy', claim, method: :delete, data: { confirm: 'Are you sure?' } %></td>-->
                  </tr>
                <% end %>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
</section>

