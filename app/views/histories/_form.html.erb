<%= form_with(model: history) do |form| %>
<div class="form-group">
            <%= form.number_field :tarjeta, class: "form-control input-lg", placeholder: "Numero de tarjeta" %>
          </div>
          <div class="form-group">
          <%= form.label :domicilio, "Envío a domicilio"%>
          <%= form.check_box :domicilio %> 
          </div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
